---
description: UI — русский текст, проверка в браузере, текущий дизайн не эталон
globs: client/**
alwaysApply: false
---

# Интерфейс

Текущий экран (Chakra, карусель 640px) — прототип. Смотрим его живьём в дорожке A, не сносим kit сейчас.

Потом весь Chakra уходит. Новый UI — **shadcn**. Не наращивать Chakra и не возвращать `@chakra-ui/icons`.
Иконки — Lucide / shadcn. Файлы kit не редактировать: обёртки снаружи.
Дорожка C: `docs/roadmap.md`.

## Текст

Ошибки, пустые состояния, тосты, кнопки — по-русски. Не пускать в UI сырой GraphQL/Prisma/English (`Error`, `Required`).

## Правки

- Менять только то, о чём задача. Не «顺便» переписывать тему и меню.
- Play, фильтры и модалки сейчас заглушки: если трогаешь — либо подключи поведение, либо не делай вид, что они работают.
- После UI-правок проверить в браузере сам сценарий (клик, скролл, пустой список, ошибка API), не один скриншот. Если браузера нет — сказать, чем проверял.

Не добавлять тесты на React/стили без просьбы.
