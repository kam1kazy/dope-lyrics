{
    "version": 2,
    "builds": [
        {
            "src": "pages/**/*.{js,ts,jsx,tsx}",
            "use": "@vercel/static-build"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "pages/$1"
        }
    ]
}