{
    "cleanUrls": true,
    "framework": "nextjs",
    "buildCommand": "next build",
    "installCommand": "bun run install",
    "outputDirectory": "out",
    "rewrites": [
        {
            "source": "out/(.*)",
            "destination": "/index.html"
        }
    ]
}