## 1. Заход 1 — сервер

- [x] 1.1 Хелпер выреза сырых диапазонов и сборки текста кусков в `server/src/modules/lyrics/lyric-text.ts` (или рядом)
- [x] 1.2 Мутация `sliceLyric` в `lyrics.service.ts`: создать запись, Оставить/Вырезать, `isDonor`/`isHidden`, remap коллажей через `lyric-glue`
- [x] 1.3 GraphQL: `LyricLineRange`, `SliceLyricPayload`, `sliceLyric` в `typeDefs.ts` / `resolvers.ts`
- [x] 1.4 Тесты выреза и remap (один и несколько ranges) рядом с `lyric-glue.test.ts`

## 2. Заход 1 — клиент

- [x] 2.1 Мутация `SLICE_LYRIC` и обновление кэша списка в `client/src/entities/lyric/`
- [x] 2.2 «+» / вторая черта и подсветка зоны в `split-text-view.tsx` + хелперы в `lyric-text-lines.ts`
- [x] 2.3 В `message-desk-dialog.tsx`: при двух чертах «Разделить» → Оставить/Вырезать → `sliceLyric` с одним range; одна черта без изменений

## 3. Заход 1 — проверка

- [x] 3.1 Одна черта режет надвое; «+» → середина → Вырезать/Оставить; отмена без записи

## 4. Заход 2 — список

- [x] 4.1 Список кусков сбоку в карточке: ножницы добавляют зону, опасити взятых строк, крестик убирает
- [x] 4.2 «Сохранить» шлёт несколько `ranges` в `sliceLyric`; оверлей Оставить/Вырезать

## 5. Заход 3 — порядок

- [x] 5.1 Перестановка кусков через `framer-motion` `Reorder`
- [x] 5.2 Проверка: два куска, порядок, сохранение, донор
