## 1. Заход 1 — очередь в памяти

- [x] 1.1 `CarouselSessionProvider`: mode catalog/queue, queue, prepend; методы добавить / играть / очистить / сброс в каталог. Подключить в `client/src/app/providers.tsx`.
- [x] 1.2 `LyricList` / `Viewport`: в queue слайды из очереди; в catalog — текущий запрос плюс prepend; пустой экран после очистки; смена key при старте «играть».
- [x] 1.3 Карточка `message-desk-dialog.tsx`: полки — иконки без подписей в один ряд; ряд «Очистить» / «Добавить» / «Играть» только из меню на табе «Текст», с тултипами; с карусели и на «Профиле» скрыт.
- [x] 1.4 Решафл в `control-bar.tsx` сбрасывает сессию в catalog. Кнопки очереди работают из `catalog-lyrics-list.tsx`.
- [x] 1.5 Список меню: при непустой очереди — переключатель «В карусели» и иконка справа у фраз из очереди; фильтр показывает очередь по дате.

## 2. Заход 2 — история

- [x] 2.1 Prisma `CarouselHistory` + миграция; GraphQL: `lyricIds`, `lyricsByIds`, save / like / unlike / delete; кольцо 12 в сервисе.
- [x] 2.2 Очистка непустой своей очереди пишет снимок; решафл и шаффл каталога не пишут.
- [x] 2.3 Раздел «История» в `catalog-menu.tsx` и кнопка под решафлом через `CatalogMenuProvider`. Список: дата, 2 строки, источник, выделение, сердце, стрелка, корзина. Лайк как у генератора. Фильтр сохранённых в шапке.

## 3. Заход 3 — жесты

- [x] 3.1 Долгое нажатие раскрывает 8–12 строк и складывает обратно.
- [x] 3.2 Свайп влево показывает корзину (`data-swipe-ignore` на списке).
