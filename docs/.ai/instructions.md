Не пиши код, пока не прочитал:

1. `docs/00_vision.md`
2. `docs/01_pillars.md`
3. `docs/02_loop.md`
4. `docs/tasks/current_task.md`
5. Связанный файл из `docs/` или `openspec/changes/`

Не меняй стек и продуктовую цель без просьбы.

Не редактируй `guide/`.

Не коммить и не пушь без явной просьбы.

Если задачи нет в `docs/tasks/` и нет OpenSpec change — спроси, куда её фиксировать, не изобретай тикет в голове.
