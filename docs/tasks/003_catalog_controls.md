# 003 — Контролы каталога

Статус: закрыта
Дорожка: D

## Зачем

Без порядка, тега и поиска нельзя нормально читать эталоны и случайные строки.

## Сделано

1. Порядок: случайно / по порядку / обратно. Повторный тап по «случайному» заново перемешивает.
2. Теги — чипы, OR, пустой выбор = все.
3. Ключевые слова — подстрока без регистра.
4. Размер, межстрочие, скорость — слайдеры.
5. Drawer со свайпом вниз.
6. GraphQL: `lyrics(tags, keyword, emojis, dateFrom, dateTo, referencesOnly)`, `lyricTags`, `lyricEmojis`.
7. Полка эталонов — кнопка `referencesOnly` (#эталон / #эталon).
8. Период — два поля даты.
9. Реакции — фильтр по эмодзи OR; на слайде видны реакции первой строки.

## Не делать

- возвращать Chakra;
- править kit-файлы shadcn;
- генерация текста / GPT;
- JWT, cookie-сессии, Linux-хост.
