# Состояние

Продукт: [00_vision.md](./00_vision.md). Текущая задача: [tasks/current_task.md](./tasks/current_task.md).

Снимок: **2 сентября 2026**. Drawer фильтров живой. `bun run fix` зелёный на клиенте и сервере.

| Кусок | Сейчас |
| --- | --- |
| Код | Локально линия `main`. `origin/develop` впереди — [git.md](./git.md) |
| Контур | GraphQL + Next, данные с TG, карусель |
| UI | shadcn. Пауза по клику. Порядок / размер / скорость / теги / ключевые слова влияют на карусель. Drawer закрывается свайпом вниз |
| Фильтры | На клиенте после `lyrics`. В GraphQL пока только `limit`/`offset` |
| Типы | На клиенте `any` не было. На сервере 48 `any` убраны: история через типы mtcute, FLOOD_WAIT через `unknown` |
| Lint | `bun run fix` без ошибок и предупреждений в `client/` и `server/` |
| Auth | Нет входа. JWT/cookie/tenant не переносили |
| OpenSpec | Активного change нет |

Дальше — остаток D (фильтр в GraphQL, эмодзи, даты, эталоны). ИИ и хост — позже. Карусель = караоке, отдельный режим не делаем.
