# Состояние

Продукт: [00_vision.md](./00_vision.md). Текущая задача: [tasks/current_task.md](./tasks/current_task.md).

Снимок: **2 сентября 2026**. Автор поднял контур: бот, парсинг, seed, клиент показывает фразы.

| Кусок | Сейчас |
| --- | --- |
| Код | Локально линия `main`. `origin/develop` впереди — [git.md](./git.md) |
| Контур | GraphQL + Next на компе, данные с TG в базе, экран живой |
| Данные | `chatHistory.json` и seed сделаны (файлы в `bot-data/`, в git не входят) |
| Бот | Подключён, `bun run bot:admin` запускали |
| UI | Прототип Chakra, карусель. Play/фильтры — заглушки |
| OpenSpec | Активного change нет |

A и B закрыты по смыслу. Дальше — не редизайн и не ИИ, а **инструмент поверх каталога** (дорожка D).
