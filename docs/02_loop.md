# Цикл

Version: 0.1

---

# Как живёт автор

Пишет строку в Telegram
→ иногда ставит хештег или реакцию
→ забывает, потому что чат уезжает вверх
→ открывает Dope Lyrics
→ видит фразу снова (рандом / карусель / караоке)
→ читает вслух, откладывает, склеивает, помечает эталон
→ позже просит инструмент собрать черновик из своих же строк

---

# Как живут данные

```
чат / канал с заметками
  → user-сеанс mtcute (не бот) читает историю
  → фильтр: текст, слова, абзацы, хештеги, реакции
  → JSON → Prisma → Postgres
  → GraphQL `lyrics`
  → экран
```

Бот нужен для кнопок (`/bd`, `/chatid`, `/app`).
Сами заметки читает аккаунт.

Домен нужен только чтобы открыть Mini App внутри Telegram
или держать бота, когда комп выключен.

---

# Режимы экрана (цель, не факт)

| Режим | Зачем |
| --- | --- |
| Рандом | Наткнуться на старую строку |
| Карусель | Листать спокойно, как слайды |
| Караоке | Читать в темпе, вслух |

Сейчас в коде один fade-прототип. Три режима — дорожка C.
